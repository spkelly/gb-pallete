import * as types from '../types';

describe('Vuex Actions',()=>{
  it('RESET',()=>{})
  it('CHANGE_FILE_NAME',()=>{})
  it('CHANGE_FILE_TYPE',()=>{})
  it('DOWNLOAD',()=>{})
  it('SHIFT_CANVAS_RIGHT',()=>{})
  it('SHIFT_CANVAS_LEFT',()=>{})
  it('SHIFT_CANVAS_UP',()=>{})
  it('SHIFT_CANVAS_DOWN',()=>{})
  it('CLEAR_CANVAS',()=>{})
  it('INIT_CANVAS',()=>{})
  it('SHIFT_DATA_LEFT',()=>{});
  it('SHIFT_DATA_RIGHT',()=>{});
  it('SHIFT_DATA_UP',()=>{});
  it('SHIFT_DATA_DOWN',()=>{});
  it('UPDATE_OUTPUT',()=>{});
  it('CONVERT_PIXEL_DATA',()=>{});
  it('RESET_MATRIX',()=>{});
});